/**
 * Game-wide constants, matching the original reference game's dimensions
 * (see the "Reverse-engineer Ponpoko level map format & physics constants" ticket).
 */
export const GAME_WIDTH = 1000;
export const GAME_HEIGHT = 800;
